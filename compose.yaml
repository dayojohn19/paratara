services: 
  django:
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
      shm_size: 16g
    ports:
      - mode: ingress
        target: 8000
        # published: 8000 # uncomment for local dev
    environment:
      - DEBUG=False # set to "True" for local dev
    #deploy:
    #  resources:
    #    reservations:
    #      memory: 256M`
  # serviceName:
  #   domainname: www.gotreepnow.xyz
  #   ports:
  #   - mode: ingress
  #     target: 3000	
  #     protocol: http    